<h3>Companies</h3>

<div>
  <ul>
    <% @firms.each do |firm| %>
      <li><%= link_to firm.name, firm_path(firm) %></li>
    <% end %>
  </ul>
</div>
<div>
  <%= publishers_only do %>
    <%= link_to "New Company", new_firm_path, class: "btn btn-small" %>
  <% end %>
</div>
