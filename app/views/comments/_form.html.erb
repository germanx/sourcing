<%= form_for [@ticket, @comment] do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.text_area :text, placeholder: "New comment...", :rows => 3 %>
  </p>
  <%= f.submit nil, :class => 'btn btn-primary' %>
<% end %>